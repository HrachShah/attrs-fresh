`astuple` now forwards the *filter* argument to its recursive call when serializing attrs instances reached through `dict` values and `dict` keys, matching the behavior already shipped for `list`, `tuple`, `set`, and `frozenset` values.
