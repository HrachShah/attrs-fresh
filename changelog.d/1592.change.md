`attrs.filters.include` and `attrs.filters.exclude` now match types by `isinstance` rather than exact `type(value) == cls`, so a value whose class subclasses one of the listed types is correctly included/excluded.
[#1592](https://github.com/python-attrs/attrs/issues/1592)
