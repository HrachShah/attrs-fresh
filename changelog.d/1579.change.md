`attrs.validators.and_` and `attrs.validators.or_` now raise `NotCallableError` at the call site when given a non-callable argument, instead of silently treating the bad argument as always-valid (or always-invalid, depending on which slot it landed in).
